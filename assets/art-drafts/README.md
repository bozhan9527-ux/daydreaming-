# 美術原始圖存放區

這個資料夾專門放**你從 AI 繪圖工具生成、還沒處理過的原圖**(例如 Gemini App / Copilot 生成的 PNG)。

## 使用方式

1. 在這裡上傳原始圖檔(GitHub 網頁:進到這個資料夾 → Add file → Upload files)
2. 跟開發這邊說一聲檔名/路徑,由開發那邊裁切、去背、整理後正式移到 `assets/sprites/` 對應的子資料夾裡使用
3. 這裡的檔案是暫存草稿,正式套用進遊戲的檔案一律放在 `assets/sprites/`,不會留在這裡
